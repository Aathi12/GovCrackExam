# Analogy (Word/Number) Verification Prompt

Read the file `analogy_to_verify.json`. It contains 30 candidate questions for "Analogy (Word/Number)".

Your task is to independently solve and verify at least 15-20 of these questions.
For each question:
1. Identify the relationship/logic in the first pair (or pairs).
2. Apply the exact same logic to find the missing term or pair in the options.
3. If it is ambiguous, corrupted, or requires visual data not present, SKIP IT.
4. Create a concise explanation explicitly stating the mathematical or semantic logic linking the words/numbers.
5. The correct answer must be the 1-indexed option number (1, 2, 3, or 4).

Format your output as a markdown JSON block containing an array of objects.
Each object must have:
- `question`: The question text (cleaned up). Example: "Select the related number from the given alternatives. 12 : 144 :: 14 : ?"
- `options`: A list of exactly 4 string options (cleaned up).
- `correctOption`: Integer 1, 2, 3, or 4.
- `explanation`: The concise explanation you formulated.
- `subtopic`: "Analogy (Word/Number)"
- `difficulty`: "medium"
- `sourceYear`: 2024
- `sourcePaper`: "SSC CGL QUESTION PAPERS — EXTRACTED TEXT"

Aim to successfully verify 15-20 questions. Return the final JSON array in a markdown block.
